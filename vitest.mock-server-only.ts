/**
 * Mock for server-only package in tests
 *
 * This module is used to replace the server-only package during testing,
 * allowing server-side code to be tested without errors.
 */

// Empty export - the server-only package just throws when imported on client
export {};
